#include <stdio.h>

int main()
{
    int a, b, c;
    a = 5;
    b = 4;
    printf("The Value of a is %d\n", a);
    printf("The Value of b is %d\n", b);
    printf("Sum is %d\n", a + b);
    printf("Remainder when a id divided by b (a/b): %d\n", a % b);
    return 0;
}
/*
  Operations
    | Addition
    | Subtraction
    | Multiplication
    | Divisions
    | Modular Divisions (get remainder)
    Format
    x + y = z
    x,y are operands
    + is Operator
    z is result
*/