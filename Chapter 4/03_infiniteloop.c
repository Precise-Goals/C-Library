#include <stdio.h>

int main()
{
    int i = 0;
    // while (i < 4) {
    //     printf("Happy Birthday\n");
    //     i = i + 1;
    // }
    while (2 <= 10) { // statement always true
        printf("The Value of i is %d\n", i);
        i++; 
        // i++; // to make it infinitew
    }

    return 0;
}